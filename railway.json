{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --chdir SMS school_app.wsgi:application --bind 0.0.0.0:$PORT",
    "preDeployCommand": "python manage.py collectstatic --noinput"
  }
}
