{ 
      "build": { 
        "builder": "NIXPACKS", 
        "buildCommand": "pip install -r requirements.txt" 
      }, 
      "start": "gunicorn school_app.wsgi:application --bind 0.0.0.0:$PORT" 
    }